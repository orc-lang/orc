/**
 * Representations for Orc types which don't
 * have direct analogues in the host language (Java).
 * 
 * <p>To promote efficient interoperability and as a matter of convenience,
 * Orc adopts the value representation of its host language.  Some
 * distinguished subsets of of values are chosen as Orc's "native" types and 
 * given special meaning to the Orc interpreter.
 */
package orc.runtime.values;