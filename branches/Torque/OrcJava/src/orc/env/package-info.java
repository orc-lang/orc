/**
 * A lexical context which supports both offset- and
 * name-based lookup. This is used at compile time
 * to convert variables to DeBruijn indices, and at
 * runtime to store variable bindings.
 */
package orc.env;